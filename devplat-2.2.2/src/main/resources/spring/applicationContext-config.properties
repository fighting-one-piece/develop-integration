#Application Context Config
target-env=${target-env}

driverClassName=#{driverClassName}
url=#{url}
username=#{username}
password=#{password}
database.sid=#{database.sid}

datasource.master.url=#{datasource.master.url}
datasource.master.username=#{datasource.master.username}
datasource.master.password=#{datasource.master.password}

datasource.slave1.url=#{datasource.slave1.url}
datasource.slave1.username=#{datasource.slave1.username}
datasource.slave1.password=#{datasource.slave1.password}

initialSize=#{initialSize}
maxActive=#{maxActive}
maxIdle=#{maxIdle}
minIdle=#{maxIdle}
maxWait=#{maxWait}
connectionProperties=#{connectionProperties}
defaultAutoCommit=#{defaultAutoCommit}

# Setting For DBCP DataSource
dbcp.initialSize=#{dbcp.initialSize}
dbcp.maxActive=#{dbcp.maxActive}
dbcp.maxIdle=#{dbcp.maxIdle}
dbcp.minIdle=#{dbcp.minIdle}
dbcp.maxWait=#{dbcp.maxWait}
dbcp.removeAbandoned=#{dbcp.removeAbandoned}
dbcp.defaultAutoCommit=#{dbcp.defaultAutoCommit}
dbcp.removeAbandonedTimeout=#{dbcp.removeAbandonedTimeout}

#Setting For MongoDB
mongodb.host=#{mongodb.host}
mongodb.port=#{mongodb.port}
mongodb.dbname=#{mongodb.dbname}
mongodb.collectionname=#{mongodb.collectionname}

#Setting For Redis
redis.pool.maxActive=#{redis.pool.maxActive} 
redis.pool.maxWaitMillis=#{redis.pool.maxWaitMillis}
redis.pool.maxTotal=#{redis.pool.maxTotal}
redis.pool.maxIdle=#{redis.pool.maxIdle}
redis.pool.maxWait=#{redis.pool.maxWait}
redis.pool.minIdle=#{redis.pool.minIdle}
redis.pool.testOnBorrow=#{redis.pool.testOnBorrow}
redis.pool.testOnReturn=#{redis.pool.testOnReturn}
redis.pool.host=#{redis.pool.host}
redis.pool.port=#{redis.pool.port}
redis.pool.address=#{redis.pool.address}
redis.pool.password=#{redis.pool.password}
redis.pool.minEvictableIdleTimeMillis=#{redis.pool.minEvictableIdleTimeMillis}
redis.pool.timeBetweenEvictionRunsMillis=#{redis.pool.timeBetweenEvictionRunsMillis}

redis.cluster.timeout=#{redis.cluster.timeout}
redis.cluster.maxRedirections=#{redis.cluster.maxRedirections}

redis.cluster.address1=#{redis.cluster.address1}
redis.cluster.address2=#{redis.cluster.address2}
redis.cluster.address3=#{redis.cluster.address3}
redis.cluster.address4=#{redis.cluster.address4}
redis.cluster.address5=#{redis.cluster.address5}
redis.cluster.address6=#{redis.cluster.address6}


